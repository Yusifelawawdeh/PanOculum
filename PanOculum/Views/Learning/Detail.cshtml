@{
    Layout = null;


}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title></title>
</head>
<body>
    <div>
        <h1> 
            Hello from the Other side
        </h1>
        <h2>
            @ViewBag.LearnWhat
        </h2>
        <div>
            @Html.Raw(ViewBag.LearnHow)
        </div>
        <h3>
            @ViewBag.LearnWhen
        </h3>

        <h4>
            @ViewBag.LearnHow
        </h4>
        <div>
            <ul>
                @foreach(var curriculums in ViewBag.Curriculum)
                {
                    <li>@curriculums</li>
                }
            </ul>
        </div>
    </div>
</body>
</html>
